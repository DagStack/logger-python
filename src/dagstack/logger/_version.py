"""Single-source version for dagstack-logger.

Hatch reads `__version__` from here (see pyproject.toml `[tool.hatch.version]`).
The `vX.Y.Z` tag must match — the CI publish-workflow validates this.
"""

__version__ = "0.2.0"
